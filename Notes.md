to generate a circle 
    we take circle center point then for all pixels on screen we check if distance from circle center to point is smaller than radius then draw pixel to make more efficient we can reduce the pixel checking to the size of smallest rectangle covering the circle
 
